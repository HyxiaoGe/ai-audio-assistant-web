# V1.2版本测试指南

## 🎯 快速测试清单

### 准备工作

1. **确认环境变量**
   ```bash
   # 至少配置一个LLM服务
   export DEEPSEEK_API_KEY=sk-xxx
   # 或其他：QWEN_API_KEY, DOUBAO_API_KEY, MOONSHOT_API_KEY, OPENROUTER_API_KEY

   # 确认ASR服务配置
   export TENCENT_SECRET_ID=xxx
   export TENCENT_SECRET_KEY=xxx
   ```

2. **启动服务**
   ```bash
   # 使用Docker Compose（推荐）
   docker-compose up -d

   # 或单独启动
   uvicorn app.main:app --reload  # API服务
   celery -A worker.celery_app worker --loglevel=info  # Worker
   ```

3. **查看日志**
   ```bash
   # 实时查看worker日志
   tail -f logs/worker.log

   # 或使用Docker
   docker-compose logs -f worker
   ```

---

## 📝 测试场景

### 场景1：基础功能测试（短内容）

**目标：** 验证基础摘要生成，不触发章节划分

**步骤：**

1. **准备测试文件**
   - 5-10分钟的会议录音
   - 或使用项目中现有的测试文件

2. **上传任务**
   ```bash
   # 使用curl上传
   curl -X POST http://localhost:8000/api/v1/tasks \
     -H "Authorization: Bearer YOUR_JWT_TOKEN" \
     -F "file=@test_audio.mp3" \
     -F 'options={"summary_style": "meeting"}'

   # 记录返回的task_id
   TASK_ID="返回的任务ID"
   ```

3. **监控任务进度**
   ```bash
   # 查询任务状态
   watch -n 2 "curl -s http://localhost:8000/api/v1/tasks/$TASK_ID | jq '.data.status, .data.progress'"
   ```

4. **查看worker日志**
   ```bash
   # 应该看到以下关键日志：
   # - "Starting quality-aware summary generation"
   # - "Quality assessment: high" (或 medium/low)
   # - "Preprocessed text: XXX characters"
   # - "Skipping chapter segmentation" (短内容)
   # - "Generated overview summary"
   # - "Generated key_points summary"
   # - "Generated action_items summary"
   # - "Summary generation completed"
   ```

5. **获取摘要结果**
   ```bash
   # 获取所有摘要
   curl http://localhost:8000/api/v1/summaries/$TASK_ID | jq '.'

   # 应该看到3个摘要对象：
   # - summary_type: "overview"
   # - summary_type: "key_points"
   # - summary_type: "action_items"
   ```

6. **验证摘要格式**
   ```bash
   # 查看overview内容
   curl -s http://localhost:8000/api/v1/summaries/$TASK_ID | \
     jq -r '.data[] | select(.summary_type=="overview") | .content'

   # 应该包含：
   # - Markdown标题（# ## ###）
   # - 表格（| ... |）
   # - 列表（- ✓）
   # - 分类结构
   ```

**预期结果：**
- ✅ 任务状态：completed
- ✅ 生成3种摘要（无chapters）
- ✅ 摘要内容结构化（Markdown格式）
- ✅ 生成时间：<30秒
- ✅ 日志无ERROR级别错误

---

### 场景2：长内容章节划分测试

**目标：** 验证自动章节划分功能

**步骤：**

1. **准备长音频**
   - 30分钟以上的讲座或播客
   - 或拼接多个短音频

2. **上传任务（讲座风格）**
   ```bash
   curl -X POST http://localhost:8000/api/v1/tasks \
     -H "Authorization: Bearer YOUR_JWT_TOKEN" \
     -F "file=@long_lecture.mp3" \
     -F 'options={"summary_style": "lecture"}'

   TASK_ID="返回的任务ID"
   ```

3. **关注日志中的章节划分**
   ```bash
   tail -f logs/worker.log | grep -E "chapter|segment"

   # 应该看到：
   # - "Chapter segmentation starting"
   # - "Generated X chapters"
   # - "Chapter 1: [标题]"
   ```

4. **检查数据库**
   ```sql
   -- 查询生成的摘要
   SELECT task_id, summary_type, LENGTH(content) as content_length, model_used
   FROM summaries
   WHERE task_id = 'YOUR_TASK_ID';

   -- 应该看到4条记录：
   -- overview, key_points, action_items, chapters
   ```

5. **查看chapters内容**
   ```bash
   curl -s http://localhost:8000/api/v1/summaries/$TASK_ID | \
     jq -r '.data[] | select(.summary_type=="chapters") | .content'

   # 应该是JSON格式，包含：
   # {
   #   "total_chapters": 4,
   #   "chapters": [
   #     {
   #       "index": 1,
   #       "title": "章节标题",
   #       "start_offset": 0,
   #       "end_offset": 1500,
   #       "summary": "本章节概要"
   #     },
   #     ...
   #   ]
   # }
   ```

**预期结果：**
- ✅ 任务状态：completed
- ✅ 生成4种摘要（含chapters）
- ✅ chapters为有效JSON格式
- ✅ 章节数量：3-5个
- ✅ 章节标题简洁（5-10字）
- ✅ 生成时间：<60秒

---

### 场景3：低质量转写测试

**目标：** 验证质量感知和premium model切换

**方法A：使用嘈杂音频**

1. **准备低质量音频**
   - 背景噪音大的录音
   - 多人同时说话
   - 音质模糊

2. **上传并观察**
   ```bash
   curl -X POST http://localhost:8000/api/v1/tasks \
     -H "Authorization: Bearer YOUR_JWT_TOKEN" \
     -F "file=@noisy_audio.mp3" \
     -F 'options={"summary_style": "meeting"}'
   ```

3. **查看日志**
   ```bash
   # 应该看到：
   # - "Quality assessment: low"
   # - "Low quality transcript detected, using premium model"
   # - "Selected premium LLM: claude/anthropic/..." (如果配置了)
   ```

**方法B：模拟低质量（开发测试）**

修改 `app/utils/transcript_processor.py` 临时降低阈值：
```python
# 临时修改（仅测试用）
if avg_confidence < 0.9:  # 原本是0.7
    return TranscriptQuality(quality_score="low", ...)
```

**预期结果：**
- ✅ 日志显示检测到低质量
- ✅ 切换到premium model（如配置了高级模型）
- ✅ 生成的摘要质量仍然可接受
- ✅ quality_notice出现在提示词中

---

### 场景4：三种风格差异化测试

**目标：** 验证meeting/lecture/podcast风格输出不同

**步骤：**

1. **准备相同音频**
   - 使用同一个15分钟左右的音频

2. **分别用三种风格生成**
   ```bash
   # 会议风格
   curl -X POST http://localhost:8000/api/v1/tasks \
     -F "file=@test.mp3" \
     -F 'options={"summary_style": "meeting"}'

   # 讲座风格
   curl -X POST http://localhost:8000/api/v1/tasks \
     -F "file=@test.mp3" \
     -F 'options={"summary_style": "lecture"}'

   # 播客风格
   curl -X POST http://localhost:8000/api/v1/tasks \
     -F "file=@test.mp3" \
     -F 'options={"summary_style": "podcast"}'
   ```

3. **对比overview结构**
   ```python
   # 使用Python脚本对比
   import requests

   styles = ["meeting", "lecture", "podcast"]
   task_ids = ["task1_id", "task2_id", "task3_id"]

   for style, task_id in zip(styles, task_ids):
       resp = requests.get(f"http://localhost:8000/api/v1/summaries/{task_id}")
       overview = next(s for s in resp.json()["data"] if s["summary_type"] == "overview")
       print(f"\n=== {style.upper()} 风格 ===")
       print(overview["content"][:500])  # 打印前500字符
   ```

4. **检查差异点**
   - **会议风格**：应看到"讨论议题"、"核心决策"、"关键信息速查表"
   - **讲座风格**：应看到"核心内容"、"内容大纲"、"学习重点表"
   - **播客风格**：应看到"核心主旨"、"精彩摘录"、"关键启示表"

**预期结果：**
- ✅ 三种风格的overview结构完全不同
- ✅ key_points分类不同
- ✅ action_items的生成策略不同

---

## 🔍 常见问题排查

### 问题1：任务失败，状态为failed

**排查步骤：**

1. 查看任务详情
   ```bash
   curl http://localhost:8000/api/v1/tasks/$TASK_ID | jq '.data.error_message'
   ```

2. 查看worker日志
   ```bash
   tail -100 logs/worker.log | grep -A 20 "Task $TASK_ID"
   ```

3. 常见原因：
   - LLM API key未配置或失效
   - ASR服务配额用完
   - 音频格式不支持
   - 网络连接问题

---

### 问题2：生成的摘要是空的

**排查步骤：**

1. 检查数据库
   ```sql
   SELECT summary_type, LENGTH(content), content
   FROM summaries
   WHERE task_id = 'YOUR_TASK_ID';
   ```

2. 查看LLM返回
   ```bash
   # 日志中搜索LLM响应
   grep "LLM response" logs/worker.log
   ```

3. 可能原因：
   - LLM返回为空（模型问题）
   - 提示词格式错误
   - max_tokens设置过小

---

### 问题3：章节划分返回的不是JSON

**排查步骤：**

1. 查看chapters内容
   ```bash
   curl -s http://localhost:8000/api/v1/summaries/$TASK_ID | \
     jq -r '.data[] | select(.summary_type=="chapters") | .content'
   ```

2. 如果不是有效JSON，查看日志
   ```bash
   grep "Chapter segmentation" logs/worker.log -A 30
   ```

3. 可能原因：
   - LLM未严格遵循JSON格式
   - 提示词需要调整
   - 文本太长导致截断

**临时修复：**
- 增加max_tokens（在segmentation/config.json中）
- 调整提示词强调JSON格式
- 添加JSON提取逻辑

---

### 问题4：生成时间过长（>2分钟）

**排查步骤：**

1. 查看任务进度
   ```bash
   watch -n 2 "curl -s http://localhost:8000/api/v1/tasks/$TASK_ID | jq '.data.progress, .data.status_message'"
   ```

2. 检查卡在哪个阶段
   - extracting：视频转换慢
   - transcribing：ASR服务慢
   - summarizing：LLM服务慢

3. 优化方案：
   - 使用更快的LLM服务
   - 减少max_tokens
   - 启用并行生成（未来优化）

---

## 📊 性能基准

### 正常情况下的性能指标

| 音频时长 | ASR时间 | 摘要时间 | 总时间 | Token消耗 |
|---------|---------|---------|--------|----------|
| 5分钟   | 10-20秒 | 15-25秒 | 30-50秒 | ~4000 |
| 15分钟  | 30-60秒 | 20-30秒 | 60-120秒 | ~5000 |
| 30分钟  | 60-120秒 | 30-50秒 | 120-200秒 | ~6500 |
| 60分钟  | 120-240秒 | 40-60秒 | 200-360秒 | ~8000 |

**注意：** 实际时间受多种因素影响（网络、服务负载、音频质量等）

---

## 🎓 测试技巧

### 1. 快速验证流程
```bash
# 一键测试脚本
cat > quick_test.sh << 'EOF'
#!/bin/bash
set -e

echo "🚀 Starting V1.2 quick test..."

# 上传测试任务
RESPONSE=$(curl -s -X POST http://localhost:8000/api/v1/tasks \
  -H "Authorization: Bearer $JWT_TOKEN" \
  -F "file=@$TEST_AUDIO" \
  -F 'options={"summary_style": "meeting"}')

TASK_ID=$(echo $RESPONSE | jq -r '.data.id')
echo "📝 Task ID: $TASK_ID"

# 等待完成
echo "⏳ Waiting for completion..."
while true; do
  STATUS=$(curl -s http://localhost:8000/api/v1/tasks/$TASK_ID | jq -r '.data.status')
  echo "   Status: $STATUS"

  if [ "$STATUS" = "completed" ] || [ "$STATUS" = "failed" ]; then
    break
  fi

  sleep 5
done

# 获取结果
if [ "$STATUS" = "completed" ]; then
  echo "✅ Task completed!"
  echo "📊 Summaries:"
  curl -s http://localhost:8000/api/v1/summaries/$TASK_ID | \
    jq '.data[] | {type: .summary_type, length: (.content | length)}'
else
  echo "❌ Task failed!"
  curl -s http://localhost:8000/api/v1/tasks/$TASK_ID | jq '.data.error_message'
fi
EOF

chmod +x quick_test.sh
./quick_test.sh
```

### 2. 批量测试不同风格
```python
# batch_test.py
import requests
import time

BASE_URL = "http://localhost:8000/api/v1"
TOKEN = "YOUR_JWT_TOKEN"
AUDIO_FILE = "test.mp3"

styles = ["meeting", "lecture", "podcast"]
results = {}

for style in styles:
    print(f"\n🧪 Testing {style} style...")

    # 上传任务
    files = {"file": open(AUDIO_FILE, "rb")}
    data = {"options": f'{{"summary_style": "{style}"}}'}
    headers = {"Authorization": f"Bearer {TOKEN}"}

    resp = requests.post(f"{BASE_URL}/tasks", files=files, data=data, headers=headers)
    task_id = resp.json()["data"]["id"]

    # 等待完成
    while True:
        status_resp = requests.get(f"{BASE_URL}/tasks/{task_id}", headers=headers)
        status = status_resp.json()["data"]["status"]

        if status in ["completed", "failed"]:
            break

        time.sleep(5)

    # 获取结果
    if status == "completed":
        summary_resp = requests.get(f"{BASE_URL}/summaries/{task_id}", headers=headers)
        results[style] = summary_resp.json()["data"]
        print(f"✅ {style} completed: {len(results[style])} summaries")
    else:
        print(f"❌ {style} failed")

# 对比结果
print("\n📊 Comparison:")
for style, summaries in results.items():
    overview = next(s for s in summaries if s["summary_type"] == "overview")
    print(f"\n=== {style.upper()} ===")
    print(overview["content"][:300] + "...")
```

### 3. 监控关键指标
```bash
# monitor.sh - 实时监控
watch -n 5 '
echo "=== Task Status ==="
curl -s http://localhost:8000/api/v1/tasks | jq ".data[:5] | .[] | {id, status, progress}"

echo "\n=== Recent Logs ==="
tail -20 logs/worker.log | grep -E "Task.*:|ERROR"
'
```

---

## ✅ 测试完成标准

测试通过的标准：

- [ ] 短内容生成3种摘要，格式正确
- [ ] 长内容生成4种摘要（含chapters）
- [ ] 低质量音频能够正常处理
- [ ] 三种风格输出明显不同
- [ ] 成功率 >95%（10次测试中至少9次成功）
- [ ] 平均生成时间符合预期
- [ ] 无ERROR级别日志错误
- [ ] 数据库数据正确保存

---

**文档版本：** V1.0
**创建日期：** 2025-01-15

祝测试顺利！🎉
